<UserControl x:Class="RPGHelper.Client.Views.HeroesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RPGHelper.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640">
    <Grid Background="Yellow">
        <Grid.RowDefinitions>
            <RowDefinition Height="44*"/>
            <RowDefinition Height="373*"/>
            <RowDefinition Height="63*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="textBox" Text="All Heroes" Grid.Row="0" Grid.Column="0" FontSize="22" FontWeight="Bold" TextAlignment="Center" Background="Coral"/>

        <ListView x:Name="HeroesList" FontSize="18" Grid.Row="1" ItemsSource="{Binding}" IsSynchronizedWithCurrentItem="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"></GridViewColumn>
                    <GridViewColumn Header="Gold" DisplayMemberBinding="{Binding Gold}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Label Grid.Row="0" Grid.Column="1" ContentStringFormat="{}{0} Stats:" Content="{Binding Name}" />
        <StackPanel Grid.Row="1" Grid.Column="1">
             <TextBlock Text="{Binding Path=HeroStats.Hp , StringFormat='Hp: {0}'}"></TextBlock>
            <TextBlock Text="{Binding Path=HeroStats.Mana, StringFormat='Mana: {0}'}"></TextBlock>
            <TextBlock Text="{Binding Path=HeroStats.AttackPower, StringFormat='AttackPower: {0}'}"></TextBlock>
            <TextBlock Text="{Binding Path=HeroStats.Defence, StringFormat='Defence: {0}'}"></TextBlock>
        </StackPanel>

        <Button x:Name="AddHero" Content="Add New Hero" FontSize="18" Click="AddHero_Click" Grid.Row="2"/>

    </Grid>
</UserControl>
